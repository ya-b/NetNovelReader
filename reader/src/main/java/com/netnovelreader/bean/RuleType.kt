package com.netnovelreader.bean

enum class RuleType {
    hostname,
    catalogSelector,
    chapterSelector,
    catalogFilter,
    chapterFilter,
    searchUrl,
    redirectFileld,
    redirectUrl,
    noRedirectUrl,
    redirectName,
    noRedirectName,
    redirectImage,
    noRedirectImage,
    charset
}